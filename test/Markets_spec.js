const chai, {expect} = require('chai');
const mocha = require('mocha');

describe("USDA Markets Test Suite", () => {
    describe("Markets by Postal Code", () =>{
        it("Should return a list of markets in proximity to the postal code", done => {
            done();
        });
    });
    describe("Markets by Lat/Long", () =>{
        it("SHould return a list of markets in proximity to the Lat/Lng", done => {
            done();
        });
    });
});